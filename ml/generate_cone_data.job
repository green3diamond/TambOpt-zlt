#!/bin/bash
#SBATCH -p shared         # partition. Remember to change to a desired partition
#SBATCH --mem=100g        # memory in GB
#SBATCH --time=04:00:00 # time in HH:MM:SS
#SBATCH -c 18            # number of cores

module load python 

conda activate multiproc_env

cd ~/tambo/TambOpt

python generate_cone_data.py
