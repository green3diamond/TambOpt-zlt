**/.ipynb_checkpoints/*
**/Untitled*.ipynb
.idea/*
.vscode/*
*.log
__pycache__/
.pycache/
.DS_Store
*.pyc

outputs/
slurm_logs/
pipeline_status*.json
slurm-*.out

decay_locations/*.h5
decay_locations/malata.jld2

# Generated figures live outside the repo (modules.constants.FIGURES_DIR);
# these two guard against them being written back here by an older --outdir.
plots/eda_outputs/
plots/paper_figures/

# Claude Code worktrees — never commit these (one was accidentally
# recorded as a gitlink; the branch itself is the durable copy)
.claude/worktrees/

# Shower packs for tests/ live with the run outputs on holylfs05, not in git
*.npz
